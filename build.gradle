plugins {
    id 'com.palantir.docker' version '0.9.0'
}

version = project.findProperty("buildVersion") ?: "0.0-dev.0"

docker {
	name 'steveneddies/rpi-dns'
	dockerfile 'src/docker/Dockerfile'
	tags ( project.version, 'latest' )
	pull true
}

